name: phonetics_poc
description: Interactive phonetics app for early learners with AI-driven lessons and cartoon animal guide.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  audioplayers: ^6.1.0
  record: ^5.0.0
  path_provider: ^2.1.0
  provider: ^6.0.0
  uuid: ^4.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
